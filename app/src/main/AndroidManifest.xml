<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.detroitlabs.devicemanager">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.detroitlabs.devicemanager.MainActivity"
            android:launchMode="singleInstance" />

        <provider
            android:name="com.detroitlabs.devicemanager.data.DeviceProvider"
            android:authorities="com.detroitlabs.devicemanager"
            android:exported="false" />

        <activity android:name="com.detroitlabs.devicemanager.SyncActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.detroitlabs.devicemanager.data.RegistrationService" />
        <service android:name="com.detroitlabs.devicemanager.data.SyncingService" />
        <service android:name="com.detroitlabs.devicemanager.data.DeviceUpdateService" />
    </application>

</manifest>